// function, const

// 도형 넓이
let horizontal = 5 //가로
let vertical = 3 //세로
let base = 10 // 밑변
let height = 15 //높이

const square = (horizontal,vertical) => {
    return horizontal * vertical;
}

const triangle = (base,height) => {
    return base * height / 2;
}

console.log(square(horizontal,vertical))
console.log(triangle(base,height))

function squere (horizontal,vertical) {
    return horizontal * vertical;
}

function triangle (base,height) {
    return base * height / 2;
}

console.log(squere(horizontal,vertical))
console.log(triangle(base,height))


// 계산기

let a = 5
let b = 1

const add = (a,b) => {
    return a + b;
}

const subtract = (a,b) => {
    return a - b;
}

const multiply = (a,b) => {
    return a * b;
}

const divide = (a,b) => {
    return a / b;
}

const squared = (a) => {
    return a * a;
}

console.log(add(a,b))
console.log(subtract(a,b))
console.log(multiply(a,b))
console.log(divide(a,b))
console.log(squared(a))

function add(a,b) {
    return a + b;
}

function subtract(a,b) {
    return a - b;
}

function multiply(a,b) {
    return a * b;
}

function divide(a,b) {
    return a / b;
}

function squared(a) {
    return a * a;
}

console.log(add(a,b))
console.log(subtract(a,b))
console.log(multiply(a,b))
console.log(divide(a,b))
console.log(squared(a))
//--------------------------------------







